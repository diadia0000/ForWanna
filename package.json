{
  "name": "forager-mp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "peer": "peerjs --port 9000 --key peerjs --path /myapp",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "dexie": "^3.2.7",
    "peerjs": "^1.5.4",
    "pixi.js": "^8.4.0"
  },
  "devDependencies": {
    "peer": "^1.0.2",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^2.1.9"
  }
}
